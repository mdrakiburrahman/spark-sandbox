version: 2

seeds:
  - name: countryregion
    config:
      schema: dbt_adventureworks_seed
      column_types:
        countryregioncode: string
        modifieddate: timestamp
        name: string
