version: 2

seeds:
  - name: customer
    config:
      schema: dbt_adventureworks_seed
      column_types:
        customerid: integer
        personid: integer
        storeid: integer
        territoryid: integer
