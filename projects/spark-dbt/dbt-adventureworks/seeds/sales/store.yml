version: 2

seeds:
  - name: store
    config:
      schema: dbt_adventureworks_seed
      column_types:
        businessentityid: integer
        storename: string
        salespersonid: integer
        modifieddate: timestamp
