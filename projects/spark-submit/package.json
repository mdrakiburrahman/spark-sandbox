{
    "name": "spark-submit",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "description": "DAG-based local Spark job orchestrator (CLI + UI + API) for the spark-sandbox repo.",
    "scripts": {
        "start": "tsx index.ts",
        "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js"
    },
    "dependencies": {
        "glob": "^10.5.0",
        "tsx": "^4.19.2",
        "yaml": "^2.7.0"
    },
    "devDependencies": {
        "@types/jest": "^30.0.0",
        "@types/node": "^20.10.0",
        "jest": "^30.2.0",
        "prettier": "^3.3.3",
        "ts-jest": "^29.4.6",
        "typescript": "^5.7.3"
    }
}
